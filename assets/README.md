Place the provided logo file into this folder and (optionally) rename it to `sfc-logo.png`.

Current filename used in markup: `SFC_Logo_RGB (1).png`

To move the file from the project root into this folder, run:

```bash
mv "SFC_Logo_RGB (1).png" assets/
```

After moving, you can rename it to `sfc-logo.png` and update references if desired.